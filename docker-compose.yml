services:
  backend:
    build: .
    container_name: fitmanage-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    networks:
      - gym-network

networks:
  gym-network:
    driver: bridge